<%= render partial: 'layouts/banner', locals: { page: 'Category', information: '' } %>
<div class="container mt-3">
	<p>
		<strong>Name:</strong>
		<%= @category.name %>
	</p>

	<%= link_to '<i class="fa fa-edit"></i>'.html_safe, edit_admin_category_path(@category) %> |
	<%= link_to 'Back', admin_categories_path %>
</div>
