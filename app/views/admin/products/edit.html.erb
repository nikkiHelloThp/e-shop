<%= render partial: 'layouts/banner', locals: { page: 'Edit Product', information: '' } %>

<div class="container mt-3">

	<%= render partial: 'form', locals: { product: @product } %>

	<%= link_to 'Back', admin_products_path %>
</div>